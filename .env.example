# Copy to `.env.local` for local development. Do not commit real values.
# Fill in as backend pieces (Prisma, auth, storage) are added per roadmap.

# Database (future – Prisma)
# DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/pte_mik_hok"

# Auth (future – session / NextAuth or custom)
# AUTH_SECRET=""
# NEXTAUTH_URL="http://localhost:3000"

# Optional: file uploads / CDN (future)
# STORAGE_BUCKET=""
# STORAGE_REGION=""
